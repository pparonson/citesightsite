/// <reference types="node" />
type Options = {
    parentURL: string;
    url: string;
    config?: string;
    debug?: boolean;
};
declare const config: {
    resolve: (specifier: string, options: Options) => {
        format: string | undefined;
        url: string;
    } | undefined;
    format: (url: string, options: Options) => {
        format: string;
    } | undefined;
    transform: (source: Buffer, options: Options) => Promise<{
        source: string;
    } | undefined>;
};
export default config;
export declare const resolve: any, load: any, getFormat: any, getSource: any, transformSource: any;
